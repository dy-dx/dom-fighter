{
  "name": "dom-fighter",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "clean": "git clean -fdX dist",
    "dev": "npm run clean && tsc -w & browser-sync start --server --files '*.css' '**/*.html' 'dist/**/*'"
  },
  "devDependencies": {
    "browser-sync": "^2.26.3",
    "node-notifier": "^5.3.0",
    "tsc-watch": "dy-dx/tsc-watch#emit-error-lines",
    "tslint": "^5.11.0",
    "typescript": "^3.1.4"
  },
  "dependencies": {
    "peerjs": "^0.3.18"
  }
}
